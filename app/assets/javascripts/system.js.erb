<%# encoding: utf-8%>
$(function() {
  if ($('body').hasClass('system')) {
    $('#system-menu-link').addClass('menu-current');
    systemServicesTable_Settings = {
      'bAutoWidth':      false,
      'bLengthChange':   false,
      'bStateSave':      true,
      'oLanguage':       dataTables_lang,
      'sPaginationType': 'full_numbers',
      'aoColumnDefs':    [{'sType': 'string', 'aTargets': [0]}]
    };
    systemServicesTable = $('#system-services-table').dataTable(systemServicesTable_Settings);
    runtimesTable_Settings = {
      'bAutoWidth':      false,
      'bLengthChange':   false,
      'bStateSave':      true,
      'oLanguage':       dataTables_lang,
      'sPaginationType': 'full_numbers',
      'aoColumnDefs':    [{'sType': 'string', 'aTargets': [0]}]
    };
    runtimesTable = $('#runtimes-table').dataTable(runtimesTable_Settings);
    frameworksTable_Settings = {
      'bAutoWidth':      false,
      'bLengthChange':   false,
      'bStateSave':      true,
      'oLanguage':       dataTables_lang,
      'sPaginationType': 'full_numbers',
      'aoColumnDefs':    [{'sType': 'string', 'aTargets': [0, 1]}]
    };
    frameworksTable = $('#frameworks-table').dataTable(frameworksTable_Settings);
    var ultabs = $('ul.tabs');
    ultabs.tabs('div.panels > div.panel', {effect: 'slide', tabs: 'li'});
  }
});